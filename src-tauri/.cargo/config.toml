# Cargo build configuration for Pelagic
# Ensures the correct MinGW linker is used regardless of PATH order
# (Prevents Git's link.exe from shadowing the actual linker)

[target.x86_64-pc-windows-gnu]
linker = "C:\\msys64\\mingw64\\bin\\gcc.exe"
ar = "C:\\msys64\\mingw64\\bin\\ar.exe"
